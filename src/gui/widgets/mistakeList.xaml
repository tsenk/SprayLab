<UserControl
	x:Class="SprayLab.Widgets.MistakeList"
	xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
	xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
	xmlns:widgets="using:SprayLab.Widgets">
	<Grid>
		<Grid.RowDefinitions>
			<RowDefinition Height="Auto" />
			<RowDefinition Height="*" />
		</Grid.RowDefinitions>

		<StackPanel Grid.Row="0">
			<TextBlock Text="Mistakes" FontSize="14" FontWeight="SemiBold" HorizontalAlignment="Center" Margin="0,10,0,8" />
			<Rectangle Height="1" Fill="{ThemeResource hairline}" Margin="12,0" />
		</StackPanel>

		<ScrollViewer Grid.Row="1" Margin="12,8,12,10">
			<ItemsControl x:Name="rows">
				<ItemsControl.ItemTemplate>
					<DataTemplate x:DataType="widgets:MistakeRow">
						<Grid>
							<Grid.ColumnDefinitions>
								<ColumnDefinition Width="28" />
								<ColumnDefinition Width="*" />
							</Grid.ColumnDefinitions>
							<TextBlock Grid.Column="0" Text="{x:Bind Num}" FontSize="12" />
							<TextBlock Grid.Column="1" Text="{x:Bind Dev}" FontSize="12" HorizontalAlignment="Center" />
						</Grid>
					</DataTemplate>
				</ItemsControl.ItemTemplate>
			</ItemsControl>
		</ScrollViewer>
	</Grid>
</UserControl>
